{
  "framework": "nextjs",
  "buildCommand": "npx prisma generate && (npx prisma migrate deploy || echo 'Migrate nao necessario') && next build",
  "outputDirectory": ".next",
  "installCommand": "npm install"
}
