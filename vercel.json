{
  "framework": "nextjs",
  "buildCommand": "npx prisma generate && (npx prisma migrate deploy || echo 'Migration ja aplicada ou banco indisponivel') && next build",
  "outputDirectory": ".next",
  "installCommand": "npm install"
}
